# تقرير مراجعة توافق ملفات المستخدمين والصلاحيات

> المشروع: OpenHealthCRM
> تاريخ المراجعة: 09 سبتمبر 2026
> نطاق المراجعة: USER_FLOWS.md و SYSTEM_SIDEBAR_AUDIT.md و ROLE_CAPABILITY_GUIDE.md مع مراجعة مسارات الصلاحيات الفعلية في الكود.
> منظور المراجعة: مدير مشروع ومسؤول إنتاج وتسليم للعميل.

---

## 1. الملخص التنفيذي

الملفات الثلاثة متوافقة بدرجة جيدة في التصور العام للنظام، خصوصًا في:

- الفصل بين مستخدمي العيادة ومرضى البوابة.
- الفصل بين Super Admin وإدارة العيادة.
- اعتماد النظام على الأدوار والصلاحيات.
- وجود أدوار Owner وDoctor وNurse وBiller وPharmacist ودور الاستقبال.

لكنها ليست متطابقة بالكامل مع التنفيذ الحالي. أهم نقاط التعارض هي:

1. اختلاف تسمية دور Receptionist وCare Coordinator.
2. عدم توضيح الفرق بشكل كافٍ بين إظهار الصفحة في الـ sidebar وبين السماح الأمني الفعلي.
3. وجود اختلافات بين جداول الصلاحيات الموثقة وبعض قواعد الحماية في `src/proxy.ts` والـ sidebar.
4. بعض تدفقات المستخدم في `USER_FLOWS.md` تصف قدرات عامة، بينما التنفيذ الفعلي يفرض صلاحيات أدق على مستوى الصفحة والـ API.

### التقييم العام

- التوافق المفاهيمي: جيد.
- التوافق مع التنفيذ: يحتاج توحيدًا.
- الجاهزية كوثيقة تسليم للعميل: غير مكتملة قبل معالجة نقاط التسمية والصلاحيات.
- حالة المنتج: مناسب للعرض الداخلي والتجربة، ويحتاج ضبطًا نهائيًا قبل اعتباره مواصفة إنتاج نهائية.

---

## 2. التعارضات الرئيسية

### 2.1 Receptionist مقابل Care Coordinator

هذا هو التعارض الأوضح في المستندات والنظام.

- `USER_FLOWS.md` يستخدم تسمية `Receptionist / Care Coordinator`.
- `ROLE_CAPABILITY_GUIDE.md` يوضح أن `Receptionist` هو اسم العرض فقط، وأن الاسم المحفوظ في RBAC هو `Care Coordinator`.
- `src/lib/role-labels.ts` يحول `Care Coordinator` إلى `Receptionist` عند العرض.
- `src/proxy.ts` يستخدم الاسم المعروض `Receptionist` في قواعد حماية الصفحات.
- `src/lib/permissions.ts` يستخدم الاسم المحفوظ `Care Coordinator` في مصفوفة صلاحيات الموديولات.

### أثر التعارض

هذا الوضع يعمل تقنيًا، لكنه يسبب لبسًا في:

- وثائق العميل.
- إنشاء المستخدمين وتعيين الأدوار.
- تفسير رسائل الصلاحيات.
- تحليل سجلات التدقيق.
- اختبار الحسابات التجريبية.

### القرار المقترح

اعتماد الصيغة التالية في كل وثائق العميل:

> Receptionist هو الاسم الظاهر للمستخدم، بينما Care Coordinator هو اسم الدور الداخلي المحفوظ في RBAC.

ويجب عدم عرض الاسمين كأنهما دورين مستقلين.

---

### 2.2 الـ sidebar ليس حدًا أمنيًا

المستندان `SYSTEM_SIDEBAR_AUDIT.md` و `ROLE_CAPABILITY_GUIDE.md` يوضحان أن الـ sidebar هو فلتر استخدام وليس حدًا أمنيًا.

أما `USER_FLOWS.md` فيصف الصفحات على أنها صفحات يستطيع الدور الوصول إليها، من دون توضيح كافٍ أن:

- إخفاء الرابط لا يساوي منع الوصول.
- الحماية الحقيقية تأتي من `src/proxy.ts` وحراس الـ API.
- فتح صفحة مباشرة قد يعرض هيكل الصفحة، بينما تمنع استدعاءات البيانات أو عمليات الكتابة عند غياب الصلاحية.

### قرار التسليم

يجب أن تحتوي وثيقة الـ user flow على ملاحظة صريحة:

> الـ sidebar يحدد ما يظهر للمستخدم لتحسين تجربة الاستخدام، أما التفويض الأمني الحقيقي فيتم على الخادم من خلال حماية الصفحات وواجهات API.

---

### 2.3 اختلاف قواعد الصفحة عن جدول الصلاحيات

`ROLE_CAPABILITY_GUIDE.md` يقدم جدولًا باعتباره المصفوفة المعتمدة، بينما `src/proxy.ts` يطبق قواعد page-level محددة.

أمثلة مهمة:

- `USER_FLOWS.md` يذكر أن Owner يملك إدارة الفروع والغرف، الموظفين، الإعدادات والخطة. هذا متوافق إجمالًا.
- `USER_FLOWS.md` يذكر أن Doctor يستطيع الوصول إلى Documents وReports وAnalytics وAvailability، وهذا متوافق مع قواعد الحماية الحالية.
- `USER_FLOWS.md` يذكر أن Nurse يستطيع الوصول إلى Inventory وDocuments وReports وAnalytics، وهذا متوافق مع المصفوفة الحالية.
- Biller يملك Billing وPayments وAudit وReports وAnalytics، وهذا متوافق إجمالًا.
- Pharmacist يملك Labs وInventory وCatalogs، وهذا متوافق مع الوثيقة والكود.

لكن يوجد اختلاف مهم في مستوى التفصيل:

- جدول الصلاحيات يصف الوصول إلى الموديول.
- بعض واجهات API تفرق بين `read` و `write`.
- بعض المستخدمين يرون الصفحة لكن لا يملكون كل عمليات التعديل داخلها.

لذلك لا يكفي أن تكتب الوثيقة أن الدور "يصل إلى الصفحة"؛ يجب تحديد هل الوصول:

- قراءة فقط.
- قراءة وكتابة.
- إدارة كاملة.

---

### 2.4 الصلاحيات المكتوبة لا تعني دائمًا صلاحيات تنفيذ كل الأفعال

مثال عملي:

- Biller يستطيع رؤية المرضى والمواعيد لأغراض الفوترة.
- هذا لا يعني أنه يستطيع تعديل بيانات المرضى أو إنشاء مواعيد.
- Pharmacist يستطيع قراءة بيانات المرضى والمواعيد، لكن ذلك لا يعني امتلاكه صلاحيات تعديل السجل الطبي.
- Nurse يستطيع استخدام بيانات سريرية ومخزون، لكن بعض إعدادات العيادة تظل Owner-only.

`USER_FLOWS.md` يحتاج إلى التفريق بين:

- ما يراه المستخدم.
- ما يستطيع قراءته.
- ما يستطيع إنشاؤه أو تعديله.
- ما لا يملكه إلا Owner.

---

## 3. مقارنة الملفات الثلاثة

### USER_FLOWS.md

**وظيفته:** شرح رحلة المستخدم اليومية بشكل مبسط.

**نقاط القوة:**

- سهل القراءة.
- يغطي الأدوار الأساسية.
- يشرح تسجيل الدخول والبوابة والـ dashboard.
- يوضح الفصل بين Staff وPatient وSuper Admin.

**نقاط الضعف:**

- لا يوضح مستوى الصلاحية: قراءة أم كتابة.
- يستخدم Receptionist وCare Coordinator كأنهما تسميتان متوازيتان.
- لا يذكر بوضوح أن بعض الأفعال قد ترجع `403` رغم ظهور الصفحة أو الرابط.
- بعض العبارات عامة أكثر من التنفيذ الفعلي، مثل "manage patient workflows end-to-end" للـ Owner دون تحديد حدود كل وحدة.
- لا يذكر بعض الحالات التشغيلية المهمة مثل الصفحة المقفلة أو منع العملية عند غياب صلاحية الكتابة.

**الحكم:** وثيقة جيدة لتجربة المستخدم، لكنها تحتاج مراجعة صلاحيات قبل اعتمادها كوثيقة إنتاج.

---

### SYSTEM_SIDEBAR_AUDIT.md

**وظيفته:** تحليل شامل لحالة النظام، الصفحات، الـ sidebar، الصلاحيات، والفجوات.

**نقاط القوة:**

- الأكثر شمولًا في وصف حالة النظام.
- يربط الملاحظات بمسارات الملفات الحقيقية.
- يميز بين ما تم إصلاحه وما يزال مفتوحًا.
- يوثق الفجوات مثل الصفحات الجزئية، الترجمة، صفحات Claims وSurveys، وواجهة portal.
- يوضح أن الحماية الأمنية الحقيقية ليست في الـ sidebar فقط.

**نقاط الضعف:**

- هو تقرير تدقيق وليس وثيقة تشغيلية للمستخدم النهائي.
- يحتوي على تاريخ تغييرات ومراحل إصلاح قد يربك العميل إذا قرأه كدليل استخدام.
- بعض الملاحظات التاريخية تحمل عبارات مثل `RESOLVED` و`REVISED`، ولذلك يجب عدم اعتبار كل فقرة فيه وصفًا مباشرًا للحالة الحالية دون قراءة السياق.
- يحتاج إلى ملخص نهائي منفصل يفرق بين "الموجود الآن" و"الـ backlog".

**الحكم:** أفضل ملف للتحليل الداخلي واتخاذ قرارات المشروع، وليس أفضل ملف لتسليم رحلة المستخدم للعميل.

---

### ROLE_CAPABILITY_GUIDE.md

**وظيفته:** تحديد الأدوار ومصفوفة الـ sidebar والصلاحيات والحسابات التجريبية.

**نقاط القوة:**

- أقرب ملف لأن يكون المرجع الرسمي للصلاحيات.
- يشرح Receptionist/Care Coordinator بوضوح أفضل من باقي الملفات.
- يفرق بين صلاحيات الموديول وصلاحيات الموارد والأفعال.
- يوثق حماية الصفحات وحسابات الـ demo.
- يوضح أن Super Admin دور منصة وليس دور عيادة.

**نقاط الضعف:**

- لا يطابق دائمًا لغة `USER_FLOWS.md` المبسطة.
- يحتوي على تفاصيل تنفيذية كثيرة قد لا تناسب وثيقة العميل النهائية.
- يحتاج إلى تعريف واحد واضح لمصدر الحقيقة: هل المرجع هو `ROLE_MODULE_ACCESS` أم قواعد `src/proxy.ts` أم جدول الـ sidebar؟
- يجب إضافة مصفوفة مختصرة للقراءة والكتابة، وليس مجرد وجود الموديول.

**الحكم:** يجب اعتباره المرجع الأساسي للصلاحيات، مع تحديث `USER_FLOWS.md` ليبنى عليه.

---

## 4. مدى توافق الملفات مع الكود

### متوافق بدرجة جيدة

- Staff login عبر `/login`.
- Patient login عبر `/patient-login` مع جلسة منفصلة.
- Super Admin عبر `/super`.
- Owner كمسؤول العيادة.
- وجود Doctor وNurse وBiller وPharmacist.
- استخدام `/dashboard` كنقطة دخول لمستخدمي العيادة.
- حماية البيانات عبر organization scope وحراس API.
- الفصل بين صلاحيات المنصة وصلاحيات العيادة.

### يحتاج توحيدًا

- تسمية Receptionist وCare Coordinator.
- تحديد القراءة والكتابة لكل دور.
- وصف العلاقة بين sidebar وpage guard وAPI guard.
- توحيد قائمة الصفحات في الوثائق مع المصدر البرمجي.
- توضيح ما يحدث عند فتح رابط مباشر غير مسموح.

### فجوات منتجية يجب عدم إخفائها عن العميل

بحسب تقرير التدقيق:

- لا توجد صفحات مستقلة كاملة لـ Prescriptions وInsurance Claims وFeedback/Surveys.
- بعض أزرار بوابة المريض ما زالت disabled أو في حالة coming soon.
- صفحة Help ما زالت جزئية من ناحية الإرسال والتعريب.
- توجد حاجة إلى تحسين حالة read-only للمستخدم الذي يستطيع القراءة ولا يستطيع الكتابة.
- لوحة dashboard عامة وليست dashboard متخصصة بالكامل لكل دور.

هذه ليست بالضرورة عوائق أمام العرض التجريبي، لكنها يجب أن تظهر في قائمة الـ known limitations عند التسليم.

---

## 5. تقييم الجاهزية للتسليم

### جاهز حاليًا لـ

- عرض داخلي.
- Demo للعميل.
- اختبار رحلات المستخدم الأساسية.
- شرح نموذج الأدوار والصلاحيات.
- تشغيل عيادة تجريبية بالأدوار الستة.

### يحتاج إغلاقًا قبل Production handoff

1. اعتماد قاموس موحد للأدوار.
2. تحديث `USER_FLOWS.md` ليذكر read/write بوضوح.
3. إنشاء مصفوفة واحدة تعتبر source of truth للصلاحيات.
4. التحقق يدويًا من كل حساب Demo على كل route مهم.
5. إظهار حالة واضحة عند منع عملية بسبب نقص صلاحية الكتابة.
6. توثيق الوظائف غير المكتملة كـ known limitations.
7. إعادة تشغيل اختبارات النوع والـ lint والاختبارات والبناء بعد أي تعديل في الصلاحيات أو الوثائق المرتبطة بها.

---

## 6. القرار الإداري

### القرار الحالي

المشروع متماسك من ناحية الهيكل العام، لكنه لا ينبغي أن يسلم للعميل على أنه نظام صلاحيات نهائي بلا ملاحظات.

### التصنيف

- **Architecture:** جيد.
- **RBAC concept:** جيد.
- **Documentation consistency:** متوسط ويحتاج توحيدًا.
- **Production readiness:** يحتاج إغلاق ملاحظات محددة.
- **Client handoff readiness:** مناسب للـ demo، وغير مكتمل كتسليم إنتاج نهائي.

### الملاحظة الأهم

لا يوجد تعارض معماري خطير يمنع تشغيل النظام، لكن يوجد تعارض توثيقي وتنفيذي في تسمية الدور وفي وصف مستوى الصلاحيات. هذا النوع من التعارض قد يتحول إلى مشكلة تشغيلية عند تدريب العميل أو إضافة موظفين جدد.

---

## 7. التوصية النهائية

اعتماد `ROLE_CAPABILITY_GUIDE.md` كمرجع الصلاحيات الأساسي، ثم تحديث `USER_FLOWS.md` ليكون وثيقة رحلة مستخدم مبنية على ذلك المرجع.

الصيغة الموحدة المقترحة:

- الاسم الظاهر: Receptionist.
- الاسم الداخلي في RBAC: Care Coordinator.
- الـ sidebar: فلتر تجربة مستخدم.
- حماية الصفحات: `src/proxy.ts`.
- حماية البيانات والعمليات: حراس API والصلاحيات على الخادم.
- Owner: إدارة العيادة.
- Super Admin: إدارة المنصة فقط.

بعد هذه التعديلات يمكن اعتبار الوثائق الثلاثة متسقة وقابلة للاستخدام في التسليم، مع إبقاء قائمة الـ known limitations واضحة للعميل.
