# Progress Log — عيادات CRM Production Readiness

## Current status
- Last updated: 2026-09-11 13:35 UTC+3
- Current phase: ALL PHASES COMPLETE (P0–P5). Last 6 deploys READY on Vercel; prod health/ready OK; new booking API live with correct 404s.
- Current task: None pending — P3 e2e execution awaits CI secrets; everything else verified.
- Status: DONE

## How to resume
لو الـ session اتقطع، اقرأ الملف ده الأول، شوف "Current task"، تحقق من حالته الفعلية في الكود (هل اتنفذ فعلاً ولا لأ)، وكمل من هناك. لا تعيد تنفيذ تاسكات مكتوب جنبها [x].

التالي: لا شيء معلق — نفذ e2e عند توفر CI secrets.

Previous clinic-blueprint tracker (P0–P7 product modules) lived in this file and is largely complete except P4-T3 charge-generation policy (blocked on product decision). Do not confuse that list with the production-readiness tasks in `TASKS.md`.

## Task log
| # | Task | Phase | Status | Notes |
|---|------|-------|--------|-------|
| 1 | Build unified TASKS.md | 0 | DONE | Merged ARCHITECTURE §4–§8, TESTING_PLAN, FEATURE_RESEARCH §3 into P0–P5. |
| 2 | Force ENCRYPTION_KEY + NEXTAUTH_SECRET on production boot | P0 | DONE | `src/lib/boot-env.ts` + `src/instrumentation.ts`. Fails if missing, <32 chars, or placeholder. Dev unchanged. Tests: `tests/unit/boot-env.test.ts` (11 with crypto). |
| 3 | Reject patient sessions when the organization is suspended | P0 | DONE (verified, pre-existing) | Gate already in `src/lib/patient-auth.ts` (`isPatientOrganizationActive`, `findActivePatientSession` org filter) + login reject in `src/app/api/patient-auth/login/route.ts`; all resolvers via `getPatientSessionFromRequest`, no bypasses. Tests: `tests/unit/patient-auth.test.ts` 3/3 green. Marked `[x]` in TASKS.md. |
| 4 | Scope `Patient.mrn` uniqueness per organization | P0 | DONE + APPLIED | Migration `20260910223000_scoped_patient_mrn_unique` applied to Neon via `migrate deploy` (`migrate status`: up to date); DB-verified: `Patient_organizationId_mrn_key` UNIQUE on ("organizationId", mrn) exists, 0 duplicate pairs. Seeds switched to `organizationId_mrn` upsert. 7/7 patient tests green. |
| 8 | P0 full verification (all 6 tasks) | P0 | DONE | Full unit suite: **35 files / 181 tests green** (`npx vitest run`). Boot gate wired via `src/instrumentation.ts`. `vercel.json` crons (hourly reminders, minutely scheduled) match `CRON_SECRET` Bearer guard. No commit (tree shared with concurrent P1/Cloudinary work). |
| 5 | Rate limiter fail-closed when Redis is unavailable | P0 | DONE (verified) | `takeRateLimitToken` catch returns `allowed:false` ("failing closed"); enforced in `proxy.ts` → 429 with Retry-After; no other fail-open paths (single store entry point). Tests: `tests/unit/rate-limit.test.ts` 17/17 green. Marked `[x]` in TASKS.md. |
| 6 | Protect public cron/webhook endpoints with CRON_SECRET | P0 | DONE (verified) | `authorizeCronRequest` (`src/lib/cron-auth.ts`): 503 if unconfigured, 401 on mismatch, `x-cron-secret`/`Bearer`; wired into `communications/scheduled` + `appointment-reminders`; Stripe webhook = signature verification equivalent; `CRON_SECRET` added to `.env.example`; stale fail-open comment fixed. Tests: `tests/unit/cron-auth.test.ts` 3/3 green. Marked `[x]` in TASKS.md. |
| 7 | Add `/api/health` and `/api/ready` | P0 | DONE (verified) | Liveness `src/app/api/health/route.ts` (`{status:"ok"}`, no deps); readiness `src/app/api/ready/route.ts` (DB `SELECT 1` → 200/503, no secrets); both `force-dynamic` + public in `proxy.ts`. Tests: `tests/unit/health.test.ts` 3/3 green. Marked `[x]` in TASKS.md. P0 COMPLETE. |
- New task batch started: TASKS_FEATURES.md — 2026-09-11 (feature-checklist execution; task 0 first, one task at a time + tests after each).
| F0 | Restore `appointment_active_slot_unique` DB index + race-condition integration test | P0-urgent | DONE 2026-09-11 | Migration `20260911120000_restore_appointment_slot_unique` deployed; `migrate status` clean. Found live double-booking (same patient/provider/slot, 16s apart) — merged reversed B→A (A coherent, B seed-contaminated): 8 rows re-pointed (UPDATE-only, patientId-guarded), B-101 cancelled w/ reason, 2 system AuditLogs, invoice sums verified unchanged. Tests: integration 5/5 green (slot-unique 2/2 + tenant 3/3), unit 42/42, tsc+eslint clean. |
| F1 | P0#1 patient merge + server search (TASKS_FEATURES.md) | P0 | DONE 2026-09-11 | Search: `GET /api/patients?q=&status=&page=&pageSize=` → `{data,total,page,pageSize,pageCount}` only with params, legacy array otherwise. Merge: `POST /api/patients/[id]/merge` (`patientMergeSchema`) → `mergePatientRecords` (19 tables + contact conflict + session revoke) + archive + audit; UI `MergePatientsDialog` in patient rows + ar/en keys. Tests: merge 3/3, search 4/4, parity 8/8, foundation 2/2, validations 7/7 (24/24); tsc+eslint clean. |
| F2 | P0#2 staff refunds + cash/transfer methods (TASKS_FEATURES.md) | P0 | DONE 2026-09-11 | No migration (`paymentMethod` pre-exists). Manual record (cash/transfer/check/insurance → completed + atomic invoice credit) in `POST /api/payments` (`method` enum, default card); `POST /api/payments/[id]/refund` (Stripe for cards, local reversal for manual, idempotent, partial supported); `resolveInvoiceStatus` helper in `src/lib/payments.ts`; GET adds `paymentMethod`/`refundedAmount`; UI: method select in `PaymentDialog`, method column + 2-step refund button in payments page + ar/en keys. Tests: manual-refund 4/4, billing 4/4, parity 8/8, nav 4/4 (20/20); tsc clean, eslint 0 errors. |
| F-B4 | Local payment gateway (Paymob/Fawry/InstaPay) | P0 | BLOCKED | Needs merchant account + vendor choice (Paymob vs Fawry vs InstaPay). Split from F2; no code until credentials/decision. |
| F3 | P0#3 installments + coupons (TASKS_FEATURES.md) | P0 | DONE 2026-09-11 | Migration `20260911130000_installments_coupons` deployed (3 new tables + 2 nullable/defaulted Invoice cols, zero data impact). `InstallmentPlan` + dues + pay (atomic Payment+invoice+due) + cancel; coupons CRUD + `couponCode` order discount at invoice create (percent/fixed, capped, expiry-checked). UI: coupon field in NewInvoiceDialog, per-invoice Installments dialog (create/pay). Tests: installments-coupons 7/7, billing 4/4, parity 8/8, nav 4/4 (23/23); tsc+eslint clean. |
| F4 | P0#4 inventory expiry/batch + server alerts (TASKS_FEATURES.md) | P0 | DONE 2026-09-11 | Migration `20260911140000_inventory_expiry_batch` deployed (2 nullable cols + index, zero data impact). `expiryDate`/`batchNumber` on create + `PATCH /api/inventory/[id]` (master data only, quantity via transactions); `GET /api/inventory/alerts` + cron `POST` (daily digest Notifications, deduped per org/day); shared `categorizeInventoryAlerts` lib; UI: dialog fields, expiry/batch columns, server-driven banners + ar/en keys. Tests: inventory-expiry 4/4, parity 8/8, hardened 10/10 (22/22); tsc+eslint clean. |
| F5 | P0#5 printable document templates + server PDF (TASKS_FEATURES.md) | P0 | DONE 2026-09-11 | No migration. `@react-pdf/renderer` added; 6 templates (referral/medical_report/lab+imaging_request/discharge/sick_leave) rendered server-side from live clinical data; `POST /api/documents/generate` (Cloudinary persist + audit, direct-download fallback when unconfigured) + `GET /api/documents/templates`; enum extended; UI Generate dialog + type labels + ar/en keys. Tests: templates 4/4 (all render %PDF), cloudinary 16/16, parity 8/8 (28/28); tsc+eslint clean. |
| F6 | P0#6 2FA + backup (TASKS_FEATURES.md) | P0 | DONE 2026-09-11 | Migration `20260911150000_totp_2fa` deployed (4 nullable/defaulted User cols, zero impact). TOTP via otplib v13 (encrypted secret, ±30s window) + single-use hashed backup codes; setup/verify/disable/status APIs + login gate (`2FA_REQUIRED` → 2-step login UI, wrong code fails closed); `/security` page (sidebar + account menu) + Owner-only `/api/org/export` JSON snapshot (16 tables, caps, sha256, audited; live-DB restore stays Neon-PITR by design). Tests: two-factor-backup 7/7, parity 8/8, nav 4/4 (19/19); tsc+eslint clean. |
| F7 | P0#7 day calendar + queue actions + waitlist booking (TASKS_FEATURES.md) | P0 | DONE 2026-09-11 | No migration. Day agenda view (date picker + time-ordered cards reusing edit/cancel actions); `POST /api/queue/actions` (call-next/complete/no-show, head-of-line or targeted, transition-guarded + audited) + queue page buttons; `POST /api/waitlist/[id]/book` (conflict-checked atomic appointment + `booked` status) + Book dialog + status labels + ar/en keys. Tests: queue-waitlist 6/6, appointments 13/13, parity 8/8, nav 4/4 (18/18 + more); tsc+eslint clean. |
| F8 | P0#8 reminder cadences + quiet hours + delivery status (TASKS_FEATURES.md) | P0 | DONE 2026-09-11 | No migration (settingsJson). `reminderConfig` (24h/1h toggles, channel toggles, quiet window) in org settings + validation; cron rewritten to honor config (per-cadence dedupe tags, quiet-skip retries next run, per-channel sent/failed rows = delivery status); settings notifications UI + ar/en keys. Tests: reminder-cadence 7/7, reminders 1/1, validations 7/7, parity 8/8 (23/23); tsc+eslint clean. |
| F9 | P0#9 staff page + shifts + branch filter (TASKS_FEATURES.md) | P0 | DONE 2026-09-11 | Migration `20260911160000_staff_shifts` deployed (new table, zero impact). Staff directory page (roles, branch filter, assign-role dialog) + Shift CRUD APIs (overlap-guarded, branch-aware, Owner-write) + rota UI per member + nav + ar/en keys; `?branchId` on staff GET. Full per-route branch-RBAC left as follow-up. Tests: shifts 5/5, staff 2/2, nav 4/4, parity 8/8 (19/19); tsc+eslint clean. |
| F10 | P0#10 expenses ledger + net profit (TASKS_FEATURES.md) | P0 | DONE 2026-09-11 | Migration 20260911170000_expenses deployed (new table, zero impact). Expense CRUD APIs (billing guards, branch-aware, audited) + netProfit helper; net wired into analytics KPIs + monthly summary; billing expenses card + analytics/report metrics + ar/en keys. Tests: expenses 3/3, analytics 2/2, billing 4/4, parity 8/8 (17/17); tsc+eslint clean. P0 BATCH COMPLETE. |
| F11 | P1#1 clinical templates + SOAP UI (TASKS_FEATURES.md) | P1 | DONE 2026-09-11 | Migration 20260911180000_clinical_templates deployed (new table, zero impact). Template CRUD APIs (encounters guards, audited) + structured SOAP composer + template picker (empty-fill only) + template manager card + ar/en keys. Tests: clinical-templates 5/5, parity 8/8, validations 7/7 (20/20); tsc+eslint clean. P1 batch started. |
| F-FLAG1 | CLINICAL REVIEW: duplicate active Concor 5mg prescriptions (both sentToPharmacy) for أحمد سيد مصطفى + vitals conflict (152/94 vs 132/82) + foreign-patient seed records under encounter E_B (B01.9, Khaled X-ray) | P0-safety | OPEN | Needs doctor/owner review. Seed-contamination cleanup (detach B01.9 + X-ray from E_B) proposed as separate task. |
| F-B1 | Public marketplace / SEO doctor pages direction | P2 | BLOCKED | Needs business decision (B2C acquisition vs ops-only). TASKS_FEATURES.md P2. |
| F-B2 | Native mobile apps (iOS/Android) | P2 | BLOCKED | Needs business decision (PWA suffices for now). TASKS_FEATURES.md P2. |
| F-B3 | Extra locales/loyalty program + real insurance clearinghouse vendor | P2 | BLOCKED | Needs business/product decision (vendor + scope). TASKS_FEATURES.md P2 + 🟡 completions. |
