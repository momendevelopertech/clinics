# قصص المستخدم حسب الدور — OpenHealthCRM (User Stories by Role)
> المصدر الوحيد: نفس بيانات صفحة `/roles-guide` في النظام (`src/lib/roles-guide-data.ts`) — متطابقان 100%.
> كل خطوة وراها زرار/route/component حقيقي من الكود. تاريخ التوثيق: 2026-09-14.

## الأدوار المغطاة
Owner (المالك) · Super Admin (السوبر أدمن) · Doctor (الدكتور) · Nurse (الممرضة) · Receptionist / Care Coordinator (الاستقبال) · Biller (المحاسب) · Pharmacist (الصيدلي) · Patient (المريض)

> ملاحظة الأسماء: الدور المحفوظ `Care Coordinator` يُعرض `Receptionist` (src/lib/role-labels.ts + proxy). حقل `User.role` نص حر (superAdmin/owner/doctor/receptionist) بلا enum — الأدوار التفصيلية عبر جداول Role/UserRole.

---

## 1. المالك (Owner)

**بروفايل:** صاحب العيادة ومديرها العام — كل الصلاحيات داخل مؤسسته.
**أول شاشة:** لوحة `/dashboard` العامة (care-board).

### الموديولز المتاحة: كل الـ 25 موديول ✅

### المهام (10)

#### 1) تعديل إعدادات العيادة واللوجو
- **هيلاقيها فين:** صفحة `/settings` — زرار Save (settings_saveChanges).
- **الخطوات:**
  1. يفتح `/settings` ويختار القسم (عام/فوترة/فريق/تنبيهات/نماذج دخول).
  2. يعدل: مدة الموعد، أوقات العمل، العملة، الضريبة، بادئة الفواتير، التذكيرات (24h/1h والقنوات وquiet hours)، سياسة الإلغاء.
  3. للوجو: `CloudinaryImageUpload (purpose=clinic_logo)` ثم Save.
  4. يدوس Save فينفذ `PATCH /api/settings`.
- **النظام بيعمل إيه:** فحص `requireOwner` ثم Zod (`src/lib/validations/settings.ts`) ثم حفظ `settingsJson` والعملة، حذف اللوجو القديم، وتدقيق `UPDATE OrganizationSettings`.
- **النتيجة:** نفس الصفحة + toast. (اسم/عنوان العيادة عرض فقط.)

#### 2) إدارة الطاقم وإسناد الأدوار
- **هيلاقيها فين:** صفحة `/staff` — زرار `staff_assignRole` ثم `staff_assign`.
- **الخطوات:**
  1. يفتح `/staff` ويرى الدليل.
  2. يدوس `staff_assignRole` فيختار العضو (`staff_member`) والدور (`staff_role`).
  3. يدوس `staff_assign` فينفذ `POST /api/staff/roles` بالـ `{userId, roleId}`.
  4. للبيانات التشغيلية: `PATCH /api/staff` بالـ `{userId, profile}`.
- **النظام بيعمل إيه:** `requireOwner` للكتابة، `UserRole.upsert`، وتدقيق التحديثات.
- **النتيجة:** الدور يظهر جنب العضو فورًا.

#### 3) نوبات الطاقم (rota)
- **هيلاقيها فين:** صفحة `/staff` — بطاقة النوبات وحقول `staff_weekday/from/to/branch` وزرار `staff_addShift`.
- **الخطوات:**
  1. يختار العضو واليوم (0-6) والوقتين والفرع.
  2. يدوس `staff_addShift` فينفذ `POST /api/shifts`.
  3. للحذف: `common_delete` بتأكيد `common_confirmDelete` فينفذ `DELETE /api/shifts/[id]`.
- **النظام بيعمل إيه:** `requireOwner` + معاملة SERIALIZABLE ترفض التداخل (409) + تدقيق.
- **النتيجة:** القائمة تتحدث.

#### 4) عرض الخطة وطلب ترقية
- **هيلاقيها فين:** صفحة `/plan` — زرار `plan_upgrade` (للمالك فقط).
- **الخطوات:**
  1. يرى الخطة والمزايا (`GET /api/plan/entitlements`).
  2. يدوس `plan_upgrade` مع ملاحظة فينفذ `POST /api/org/request-upgrade`.
- **النظام بيعمل إيه:** `requireOwner`، رفض نفس الخطة، حفظ الطلب، وتدقيق.
- **النتيجة:** بانر معلق + الطلب في موافقات السوبر.

#### 5) مفاتيح API والـ webhooks
- **هيلاقيها فين:** صفحة `/integrations` — حقول `int_keyName` و`int_hookUrl` وأزرار الإضافة والإبطال والنسخ.
- **الخطوات:**
  1. يكتب الاسم ويدوس إضافة فينفذ `POST /api/api-keys` (scopes: `fhir:write`).
  2. ينسخ المفتاح فورًا (`int_copy`) — مرة واحدة فقط.
  3. للإبطال: `int_revoke` بتأكيد (`POST …/[id]/revoke`).
  4. للـ webhook: الرابط + الأحداث ثم إضافة (`POST /api/webhooks`)، والإيقاف/الحذف (PATCH/DELETE) بتأكيد.
- **النظام بيعمل إيه:** كل الأكشنات `requireOwner`، سر مشفر، HMAC + 3 محاولات، وتدقيق.
- **النتيجة:** الجداول تتحدث عبر `loadAll`.

#### 6) تصدير نسخة البيانات
- **هيلاقيها فين:** صفحة `/security` — بطاقة `sec_export` وزرار `sec_exportBtn`.
- **الخطوات:**
  1. يدوس الزرار فينفذ `GET /api/org/export`.
  2. يستلم `clinic-backup-<date>.json` (بدون blobs — الاسترجاع عبر Neon PITR).
- **النظام بيعمل إيه:** `requireOwner` + لقطة محدودة + sha256 + تدقيق EXPORT.
- **النتيجة:** تحميل فوري أو خطأ 403.

#### 7) الفروع والغرف (إنشاء للمالك فقط)
- **هيلاقيها فين:** صفحة `/locations` — الحقول والأزرار.
- **الخطوات:**
  1. اسم الفرع + إضافة → `POST /api/branches`.
  2. اسم/رقم الغرفة + إضافة → `POST /api/rooms`.
- **النظام بيعمل إيه:** الكتابة `requireOwner` (وإلا 403) + Zod + تدقيق.
- **النتيجة:** القوائم تتحدث. (الاستقبال يرى الصفحة لكن إضافته تفشل.)

#### 8) كتالوج الخدمات والأكواد (إدارة كاملة من الصفحة)
- **هيلاقيها فين:** صفحة `/catalogs` — قوائم + بحث + أزرار إضافة/تعديل/حذف (للمالك فقط).
- **الخطوات:**
  1. يتصفح الخدمات (code·name + price) والأكواد السريرية مع بحث وصفحات.
  2. إضافة: code + name + price (+ category) → `POST /api/catalogs?kind=service` (والسريري `?kind=clinical` بالـ `{system, code, name, category}`).
  3. تعديل: قلم التعديل يملأ الفورم ثم حفظ → `PATCH /api/catalogs/[id]?kind=`.
  4. حذف بتأكيد → `DELETE` — الخدمة المرتبطة بسجلات تُعطَّل (`active=false`) بدل حذفها.
  5. غير المالك يرى الصفحة قراءة فقط + ملاحظة "Only the Owner can manage" (أزرار الكتابة مخفية).
- **النظام بيعمل إيه:** قراءة بالموديول، كتابة `requireOwner` (وإلا 403) + Zod + تدقيق.
- **النتيجة:** القوائم تتحدث، والأسعار تُستخدم في الفواتير والأوامر والتحاليل.

#### 9) مزودو التأمين (إضافة/تعديل/تعطيل)
- **هيلاقيها فين:** صفحة `/insurance` — بطاقة Providers العلوية.
- **الخطوات:**
  1. يرى المزودين (5 مبذورين: Allianz وAXA وMedNet وNextCare وGlobeMed Egypt).
  2. إضافة: الاسم + حفظ → `POST /api/insurance/providers`.
  3. إعادة تسمية → `PATCH …/[id]` — الاسم يسري على كل السياسات القديمة (cascade).
  4. تعطيل بتأكيد → `DELETE` يقلب `active=false` (لا حذف فيزيائي).
  5. غير المالك يرى المزودين كقائمة منسدلة فقط عند إنشاء سياسة.
- **النظام بيعمل إيه:** كتابة `requireOwner` (وإلا 403)، تفرد الاسم لكل مؤسسة (تكرار 409)، وتدقيق.
- **النتيجة:** القائمة والقائمة المنسدلة يتحدثان فورًا.

#### 10) تجاوز المالك (Owner Override)
- **هيلاقيها فين:** أي صفحة سجلات — نفس أزرار التعديل/الحذف العادية (بلا واجهة خاصة).
- **الخطوات:**
  1. يفتح أي سجل في مؤسسته (حتى لو أنشأه دكتور/ممرضة/محاسب) ويدوس تعديل أو حذف — يُقبل بلا خطوة إضافية.
  2. سجل مؤسسة أخرى مرفوض تمامًا (404/403) — العزل مطلق.
- **النظام بيعمل إيه:** `requireOwner` يتجاوز شرط المُنشئ داخل نفس `organizationId` فقط + تدقيق (مثبت بـ 5 اختبارات `tests/integration/owner-override.test.ts`).
- **النتيجة:** تحديث/حذف فوري داخل مؤسسته فقط.

### حدود المالك
- لا منصة سوبر (`/super` و`/api/super/*` تتطلب Super Admin).
- لا مركز Pending Services (سوبر فقط).
- تجاوز المالك داخل مؤسسته فقط — سجلات المؤسسات الأخرى محظورة تمامًا.

---

## 2. السوبر أدمن (Super Admin)

**بروفايل:** حساب المنصة خارج أي مؤسسة — العيادات والخطط والموافقات والتدقيق والإعدادات والخدمات.
**أول شاشة:** لوحة `/super` (proxy يحوّل أي صفحة أخرى لها).

### الأقسام المتاحة: المؤسسات · الموافقات · الخطط والفوترة · تدقيق المنصة · الخدمات المعلقة · إعدادات المنصة · كتالوج الخطط (/super/plans)

### المهام (4)

#### 1) إدارة العيادات وحالتها
- **هيلاقيها فين:** تبويب المؤسسات في `/super` وأكشن الحالة.
- **الخطوات:**
  1. يفتح `/super` فتُحمّل `GET /api/super/orgs`.
  2. يغيّر الحالة (pending/active/suspended) فينفذ `POST …/[orgId]/status`.
- **النظام بيعمل إيه:** فحص مزدوج (`Super Admin` + `user.role=superAdmin`) + تدقيق.
- **النتيجة:** الصف يتحدث.

#### 2) الخطط والترقيات والتجاوزات
- **هيلاقيها فين:** درج العيادة + تبويب الموافقات.
- **الخطوات:**
  1. تغيير الخطة: `POST …/[orgId]/plan` (لازم يطابق `prisma.plan.code`) + اشتراك 30 يومًا.
  2. اعتماد ترقية: `POST …/upgrade` بالـ `{approve}`.
  3. تجاوز: `GET/POST …/override` ثم حذف `DELETE …/override/[overrideId]`.
- **النظام بيعمل إيه:** `requireSuperAdmin` + تدقيق لكل عملية.
- **النتيجة:** التفاصيل والموافقات تتحدث.

#### 3) كتالوج الخطط
- **هيلاقيها فين:** `/super/plans` — PlansManager وزرار `plans_newPlan`.
- **الخطوات:**
  1. إنشاء `POST /api/super/plans` (code, nameEn/Ar, price, billingCycle, ...).
  2. تعديل `PATCH …/[id]`.
  3. نسخ `POST …/[id]/duplicate`.
- **النظام بيعمل إيه:** `requireSuperAdmin` + 409 للتكرار.
- **النتيجة:** تظهر في `/plan` للعيادات.

#### 4) الموافقات والتدقيق والإعدادات والخدمات
- **هيلاقيها فين:** التبويبات الأربعة في `/super`.
- **الخطوات:**
  1. الموافقات: عرض (`GET /api/super/approvals`) والتنفيذ من المهام 1-2.
  2. التدقيق: `GET /api/super/audit` قراءة فقط.
  3. الإعدادات: `GET/PUT /api/super/settings`.
  4. الخدمات: بطاقات قراءة فقط من `GET /api/config/status` — بلا أزرار.
- **النظام بيعمل إيه:** `requireSuperAdmin` + تدقيق الحفظ.
- **النتيجة:** التبويبات تتحدث.

### حدود السوبر أدمن
- لا أي صفحة عيادة (تحويل إجباري لـ `/super`).
- لا كتابة في بيانات عيادة (الجلسة مقيدة بمنظمة platform-admin).

---

## 3. الدكتور (Doctor)

**بروفايل:** الطبيب المعالج — تشخيص وتوثيق وطلبات وروشتات وخطط علاج.
**أول شاشة:** DoctorBoard (مواعيده اليوم + مرضاه + روشتاته + القادم).

### الموديولز: dashboard, patients, appointments, queue, encounters, analytics, consents, audit, labs, tasks, documents, reports, availability, catalogs, help ✅

### المهام (14)

#### 1) بدء زيارة وإغلاقها
- **فين:** `/encounters` — `enc_start` + قائمة المرضى، و`enc_complete`.
- **الخطوات:** اختيار المريض + `enc_start` → `POST /api/encounters` بالـ `{patientId, encounterType}`. للإغلاق: `enc_complete` → `PATCH` بالـ `{status:completed}` (إعادة الفتح 409).
- **الخلفية:** موديول encounters + `encounters:write`؛ إنشاء in_progress/startTime، إغلاق endTime، وتدقيق.
- **النتيجة:** قائمة `enc_recent`.

#### 2) ملاحظة SOAP والقوالب
- **فين:** مساحة `/encounters` — الحقول الأربعة + `enc_addNote` + مدير القوالب.
- **الخطوات:** تطبيق قالب (يملأ الفراغ — 3 مبذورة: HTN وPediatric URI وAntenatal) أو كتابة → `enc_addNote` → `POST …/[id]/notes`. حفظ قالب عبر `enc_tplSave` → `POST /api/clinical-templates`.
- **الخلفية:** `encounters:write` + تدقيق الملاحظة والقالب.
- **النتيجة:** الملاحظات تحت الزيارة.

#### 3) العلامات الحيوية والمنحنى
- **فين:** صفحة المريض — إدخال + `VitalsTrendCard`.
- **الخطوات:** إدخال `POST /api/vitals` (الوزن/الطول/الضغط/النبض/الأكسجة/الحرارة) → مراقبة `GET /api/vitals/trend`.
- **الخلفية:** `encounters:write/patients:write` + BMI تلقائي + تدقيق.
- **النتيجة:** القائمة والرسم والإحصاءات.

#### 4) روشتة كاملة
- **فين:** `/prescriptions` — الديالوج + `rx_complete` و`rx_cancel` و`common_delete` و`rx_print`.
- **الخطوات:** New + المريض + السطور → Save → `POST` (تحذيرات حساسية استشارية). إكمال/إلغاء PATCH. حذف الملغاة فقط DELETE. طباعة `/print/prescription/[id]`.
- **الخلفية:** موديول labs + `encounters:write/patients:write` + idempotency + معاملة + تدقيق.
- **النتيجة:** الجدول والشارات.

#### 5) إرسال وتكرار ونسخ ومفضلة وقوالب خاصة
- **فين:** نفس الصفحة والديالوج.
- **الخطوات:** إرسال (`POST …/[id]/send` قناة sms/whatsapp). تكرار/نسخ (إنشاء جديد دائمًا). مفضلة (`GET …/favorites`). قوالب خاصة افتراضيًا (4 مبذورة: HTN وDiabetes مشتركتان + URI وPediatric خاصتان) — قالب الزميل بشارة shared، والحفظ فيه مفتاح مشاركة (`templateShared`). جنب اسم الدواء ↗ يفتح `/inventory` (أو تلميح Managed by Nurse / Pharmacist).
- **الخلفية:** بناء رسالة + إرسال + Communication + تدقيق (400 بلا هاتف).
- **النتيجة:** toast والقوائم.

#### 6) التحاليل: طلب وإرسال واستلام ومراجعة
- **فين:** `/labs` — الديالوجات و`LabOrdersSection`.
- **الخطوات:** اسم التحليل قائمة من الكتالوج السريري (`system=LAB` — 9 مبذورة: CBC/FBS/HbA1c/Lipid/TSH/Urine/VITD/CXR/ABDUS) أو نص مخصص + ↗ لـ `/catalogs`. إنشاء `POST /api/lab-orders` → إرسال `transmit` (ختم externalRef + FHIR) → استلام `ingest` → مراجعة `PATCH {reviewed|abnormal}`.
- **الخلفية:** labs + write guards + تدقيق.
- **النتيجة:** البطاقات مع تمييز abnormal.

#### 7) التشخيصات والمتابعات
- **فين:** صفحة المريض — `POST /api/clinical-orders`.
- **الخطوات:** تشخيص `{system, code, name}` أو متابعة `{dueDate, reason}`.
- **الخلفية:** فحص الملكية + تدقيق.
- **النتيجة:** التايم لاين.

#### 8) خطط العلاج وطباعتها
- **فين:** `TreatmentPlansSection` في صفحة المريض.
- **الخطوات:** إنشاء `{patientId, title}` (draft) → خطوات `{kind, title}` → إنهاء الخطوات → إكمال (ممنوع مع معلق) → طباعة عبر documents/generate.
- **الخلفية:** `encounters:write` إلزاميًا هنا + انتقالات + تدقيق.
- **النتيجة:** الخطط بخطواتها.

#### 9) الأوامر الإجرائية
- **فين:** صفحة المريض — `POST /api/procedure-orders`.
- **الخطوات:** إنشاء `{procedureName, ...}` → تحديث الحالة (المكتمل/الملغي نهائي).
- **الخلفية:** فحص الكتالوج + تدقيق.
- **النتيجة:** القائمة والمستندات.

#### 10) المستندات: رفع وتوليد وتحميل
- **فين:** `/documents` — الرفع والتوليد والعرض.
- **الخطوات:** رفع (`/api/uploads` ثم `POST /api/documents`) → توليد قالب (`POST …/generate`) → عرض برابط موقّع.
- **الخلفية:** documents + write + رفض الروابط الخارجية + تدقيق.
- **النتيجة:** روابط موقعة لكل صف.

#### 11) التوفر الشخصي
- **فين:** `/availability` — فورم الحفظ.
- **الخطوات:** تعديل النوع/الأيام/الأوقات → `PATCH /api/profile/availability`.
- **الخلفية:** تحديث ذاتي + تدقيق.
- **النتيجة:** تُستخدم في الحجز العام.

#### 12) الموافقات
- **فين:** `/consents` — `AddConsentDialog`.
- **الخطوات:** بحث بالمريض → تسجيل `POST /api/consents`.
- **الخلفية:** consents + write + تدقيق.
- **النتيجة:** القائمة.

#### 13) المؤشرات والتقارير
- **فين:** `/analytics` و`/reports` — KPIs والتصدير.
- **الخطوات:** عرض KPIs → تقرير شهري → CSV/XLSX/طباعة. (الجدولة البريدية للمالك فقط.)
- **الخلفية:** guards قراءة + `requireOwner` للجدولة.
- **النتيجة:** البطاقات.

#### 14) المهام والتدقيق
- **فين:** `/tasks` (إنشاء/إكمال + قائمة Type) و`/audit` (مراجعة حتى 100).
- **الخطوات:** `POST /api/tasks` بالنوع (`follow_up`/`lab_review`/`claim_followup` — النوع `follow_up` يشغّل تصعيد المتابعة cron) ثم `PATCH {completed}` → مراجعة `GET /api/audit`.
- **الخلفية:** tasks/audit guards + تدقيق.
- **النتيجة:** القوائم.

### حدود الدكتور
- لا فوترة/مدفوعات. لا مخزون/معدات. الكتالوجات قراءة فقط (الإدارة للمالك). لا إعدادات/طاقم/تكاملات/سوبر. لا جدولة بريدية.

---

## 4. الممرضة (Nurse)

**بروفايل:** التمريض — نفس السريريات كالدكتور + المخزون والمعدات حصريًا، بلا طابور وتدقيق.
**أول شاشة:** لوحة `/dashboard` العامة (care-board).

### الموديولز: dashboard, patients, appointments, encounters, analytics, consents, labs, inventory, tasks, documents, reports, availability, catalogs, help ✅

### المهام (10)

#### 1) الزيارات والملاحظات والعلامات (كالدكتور)
- **فين:** `/encounters` وصفحة المريض.
- **الخطوات:** بدء/إغلاق (`POST/PATCH /api/encounters`) → SOAP وقوالب (`POST …/notes` + clinical-templates) → علامات (`POST /api/vitals`) → منحنى (`GET …/trend`).
- **الخلفية:** نفس حماية الدكتور (`encounters` + write) + تدقيق.
- **النتيجة:** القوائم والرسوم.

#### 2) الروشتات كاملة (كالدكتور)
- **فين:** `/prescriptions` — نفس الديالوج والأزرار.
- **الخطوات:** إنشاء وإكمال/إلغاء/حذف/طباعة وإرسال ومفضلة وقوالب خاصة افتراضيًا (شارة shared + مفتاح `templateShared`) — لا فرع خاص بالدكتور في الكود. ↗ جنب الدواء يفتح `/inventory` مباشرة (يملك المخزون).
- **الخلفية:** labs + write guards + تدقيق.
- **النتيجة:** الجدول.

#### 3) التحاليل والتشخيصات وخطط العلاج والإجراءات (كالدكتور)
- **فين:** `/labs` وصفحة المريض.
- **الخطوات:** اسم التحليل قائمة كتالوج (`system=LAB` — 9 مبذورة) أو نص مخصص + ↗ لـ `/catalogs`. طلب/إرسال/استلام/مراجعة + تشخيص/متابعة + خطط (ملاحظة `encounters:write` الإلزامي) + أوامر إجرائية.
- **الخلفية:** نفس الحمايات + تدقيق.
- **النتيجة:** البطاقات والتايم لاين.

#### 4) المستندات والموافقات والمهام (كالدكتور)
- **فين:** `/documents` و`/consents` و`/tasks`.
- **الخطوات:** رفع/توليد/عرض + تسجيل موافقات + مهام مع اختيار النوع (`follow_up`/`lab_review`/`claim_followup`) من قائمة Type.
- **الخلفية:** guards + تدقيق.
- **النتيجة:** القوائم.

#### 5) المخزون حصريًا
- **فين:** `/inventory` — الإضافة والحركات والتنبيهات.
- **الخطوات:** إضافة صنف (`POST /api/inventory`) → حركة (`POST …/[id]/transaction` بلا سالب) → مراقبة (`GET …/alerts`).
- **الخلفية:** inventory + write + ذرية + تدقيق.
- **النتيجة:** الجدول والتنبيهات. (الدكتور محظور.)

#### 6) المعدات والصيانة حصريًا
- **فين:** `/equipment` — السجل والشارات والبطاقتان.
- **الخطوات:** تسجيل معدة (`POST /api/equipment`) → سجل صيانة (`POST …/[id]/maintenance`).
- **الخلفية:** inventory:write + تحديث المعايرة + تدقيق.
- **النتيجة:** الشارات (CalOk/Warning/Overdue).

#### 7-10) عرض: التوفر والتحليلات والكتالوجات + الحدود
- التوفر الشخصي (كالدكتور)، KPIs والتقارير (الجدولة للمالك)، الكتالوجات عرض فقط (الإدارة للمالك + "Only the Owner can manage").
- **حدود الممرضة:** صفحة `/queue` تُفتح (proxy) لكن API يرد 403 (بلا موديول). لا تدقيق (audit). لا فوترة/تأمين. لا اتصالات/انتظار/مواقع. لا إعدادات/سوبر.

---

## 5. الاستقبال (Receptionist / Care Coordinator)

**بروفايل:** واجهة العيادة — الحجوزات والطابور والحضور والانتظار والتواصل. (المحفوظ `Care Coordinator`.)
**أول شاشة:** ReceptionBoard (الانتظار/الحاضرون/المكتمل/no-show + طابور اليوم).

### الموديولز: dashboard, patients, appointments, queue, consents, tasks, documents, communications, locations, waitlist, help ✅

### المهام (12)

#### 1) حجز موعد — `/appointments` زرار `book_title`
1. ملء المريض والدكتور والتاريخ والوقت والنوع ثم `book_book`. 2. يُنفذ `POST /api/appointments`.
- **الخلفية:** `appointments:write` + idempotency + حد الخطة + توفر الدكتور + تعارض (409) + تدقيق.
- **النتيجة:** يظهر في القائمة واليوم والتقويم.

#### 2) تعديل/إلغاء (مع عرض الانتظار) — `appts_edit` و`appts_cancel` بتأكيد
1. تعديل الحقول ثم `appts_saveChanges` → `PATCH`. 2. إلغاء بتأكيد → Cancelled.
- **الخلفية:** فحص انتقالات (409) + تعارض (409) + `autoOfferFreedSlot` عند إلغاء مستقبلي.
- **النتيجة:** الصف + `waitlistOffers`.

#### 3) حضور/انصراف/no-show — أزرار ReceptionBoard
1. `check-in` للـ scheduled/confirmed → `POST …/check-in`. 2. `check-out` للـ in_progress فقط. 3. `no-show` للثلاث الأولى.
- **الخلفية:** `src/lib/reception.ts` (المصدر الخاطئ 409) + تدقيق.
- **النتيجة:** الجدول. (لا إتمام مباشر من scheduled.)

#### 4) الطابور المباشر — `/queue`
1. `queue_callNext` (عام أو لصف) → in_progress. 2. `queue_complete` و`queue_noShow`.
3. الكل `POST /api/queue/actions`.
- **الخلفية:** اختيار الأقدم + CAS ذري (409) + تدقيق.
- **النتيجة:** إعادة تحميل.

#### 5) الانتظار والحجز منه — `/waitlist`
1. إضافة `POST /api/waitlist` (يبدأ waiting). 2. حجز `POST …/[id]/book` (لـ waiting/offered).
- **الخلفية:** رفض الماضي (400) والتعارض (409) + قلب booked + تدقيق.
- **النتيجة:** الحالات + CSV.

#### 6) المرضى: إضافة وبحث وأرشفة ودمج — `/patients`
1. إضافة (الإلزامي firstName+lastName) → `POST /api/patients` (MRN تلقائي).
2. بحث نصي + فلترة. 3. أرشفة (`POST …/archive`) ودمج (`POST …/merge` — مسموح).
- **الخلفية:** `patients:read/write` + حد الخطة + تشفير + تدقيق (منها PatientMerge).
- **النتيجة:** تحديث القوائم.

#### 7) الرسائل — `/communications`
1. ملء المريض والقناة والنوع والمحتوى ثم `comm_send` → `POST /api/communications`.
2. المجدول يُرسل عبر cron فقط (لا زرار يدوي).
- **الخلفية:** communications + write (+ استحقاق) + سجلات per-channel + تدقيق.
- **النتيجة:** جدول الحالات.

#### 8) الحملات: مرئية لكن محظورة ⚠️
- السايدبار يعرضها لكن proxy (مالك فقط) + API (موديول campaigns غائب) يمنعان → 403 دائمًا للاستقبال.

#### 9) الموافقات — `/consents`
1. تسجيل `POST /api/consents` (مسموح).
- **الخلفية:** consents + write + تدقيق.

#### 10) المستندات — `/documents`
1. رفع (`/api/uploads` ثم `POST /api/documents`) وعرض بروابط موقعة (مسموح).
- **الخلفية:** documents + write + رفض الخارجية + تدقيق.

#### 11) الفروع: عرض فقط — `/locations`
- القراءة مسموحة، الإضافة `requireOwner` (403).

#### 12) المهام وأجندة اليوم — `/tasks` (قائمة Type) وتبويب day
1. إنشاء `POST /api/tasks` مع النوع (`follow_up`/`lab_review`/`claim_followup`). 2. متابعة الأجندة (نفس GET المواعيد).

### حدود الاستقبال
- الحملات محظورة فعليًا. إنشاء الفروع والكتالوجات للمالك. لا فوترة/روشتات/تحاليل/زيارات. لا إعدادات/سوبر. الـ cron تلقائي.

---

## 6. المحاسب (Biller)

**بروفايل:** مسؤول الإيراد — الفواتير والمدفوعات والتأمين والمصروفات والتقارير.
**أول شاشة:** لوحة `/dashboard` العامة.

### الموديولز: dashboard, patients, appointments, analytics, audit, billing, payments, tasks, reports, help ✅

### المهام (10)

#### 1) فاتورة من خدمة الكتالوج — `/billing` زرار New Invoice
1. المريض + خدمة من قائمة الكتالوج (code·name + price — السعر والوصف من الكتالوج و`serviceCatalogId` يُرسل). 2. ↗ جنب الحقل يفتح `/catalogs`. 3. الوصف اليدوي احتياطي فقط عند فراغ الكتالوج. 4. Create → `POST /api/billing/invoices` (الكوبون اختياري).
- **الخلفية:** idempotency + فحص الكوبون + draft + تدقيق.
- **النتيجة:** الجدول.

#### 2) مدفوعة — ديالوج `pay_processPayment`
1. الفاتورة والمبلغ والطريقة (card/online/cash/transfer/check/insurance) → `POST /api/payments`.
- **الخلفية:** رفض تجاوز المستحق. اليدوي يُسوّى فورًا، البطاقة PaymentIntent معلق + تدقيق.
- **النتيجة:** toast والقائمة.

#### 3) استرداد بخطوتين — `/payments`
1. `pay_refund` للتسليح ثم `pay_refundConfirm` → `POST …/[id]/refund`.
- **الخلفية:** تكرار المفتاح يُختصر + قفل متفائل (409) + Stripe idempotent + تدقيق.
- **النتيجة:** شارة refunded. (الجزئي API فقط.)

#### 4) إيصال — `/print/receipt/[invoiceId]` مباشرة + PrintButton (A5).

#### 5) تقسيط — `billing_installments` في `/billing`
1. إنشاء `POST /api/installment-plans`. 2. دفع `POST …/[id]/pay`. 3. الإلغاء API فقط.
- **الخلفية:** خطة نشطة واحدة + ذرية الدفع (409) + تدقيق.

#### 6) تأمين — `/insurance` (المزود قائمة منسدلة + ↗)
1. المزود من قائمة منسدلة (النشطون فقط — 5 مبذورين) ثم سياسة `POST …/policies`. 2. أهلية `GET …/[id]/eligibility`. 3. مطالبة `POST …/claims`. 4. تقديم الحالة `PATCH` (المخالف 409).
- **الخلفية:** الدفع يخصم من الفاتورة بحد المستحق + تدقيق. (إدارة قائمة المزودين للمالك — بطاقة Providers.)

#### 7) مصروفات — بطاقة `/billing`
1. إضافة `POST /api/expenses`. 2. حذف `DELETE …/[id]` بتأكيد.
- **الخلفية:** فحص الفرع + تدقيق. تدخل في صافي الربح.

#### 8) كوبونات — بطاقة `/billing` (إنشاء percent/fixed + تفعيل بالنقر) وحقل الفاتورة يستهلكها
1. إنشاء من البطاقة (`POST /api/coupons`) — 3 مبذورة. 2. النقر على الشارة يفعّل/يعطّل (`PATCH …/[id]`). 3. الكود في الفاتورة يُفحص ويُخصم.

#### 9) تقارير وتحليلات وتدقيق — `/reports` و`/analytics` و`/audit`
- عرض وتصدير (CSV/XLSX/طباعة). الجدولة البريدية للمالك فقط. التدقيق مسموح عبر `billing:read`.

#### 10) بحث الفواتير والمهام — `/billing` (بحث + فلترة) و`/tasks` (قائمة Type).

### حدود المحاسب
- لا زيارات/تحاليل/روشتات/مخزون — ولا يفتح `/inventory` إطلاقًا. لا طابور/اتصالات/انتظار. الباقات في `/catalogs` (خارج موديولاته). إلغاء التقسيط/الجدولة: API/مالك. لا إعدادات/سوبر.

---

## 7. الصيدلي (Pharmacist)

**بروفايل:** الدواء والمخزون — مراجعة روشتات وتحاليل + أصناف ومعدات + كتالوجات (عرض).
**أول شاشة:** لوحة `/dashboard` العامة.

### الموديولز: dashboard, patients, appointments, labs, inventory, tasks, catalogs, help ✅

### المهام (4)

#### 1) الروشتات — `/prescriptions` + الصرف
- مراجعة وبحث وطباعة. الإكمال/الإلغاء/الإنشاء تعمل فقط بمنح `encounters:write/patients:write` (وإلا 403 رغم ظهور الأزرار). الصرف من `DispenseDialog` (اختيار المخزون لكل سطر + ↗ لـ `/inventory`). القوالب خاصة افتراضيًا (شارة shared).

#### 2) التحاليل — `/labs`
- عرض وإضافة ومراجعة (بنفس حقول الدكتور) — التنفيذ حسب المنح. اسم التحليل قائمة كتالوج (`system=LAB` — 9 مبذورة) أو نص مخصص + ↗ لـ `/catalogs`. المراجعة قيمة status (لا route مستقل).

#### 3) المخزون والمعدات — `/inventory` و`/equipment`
- أصناف وحركات وتنبيهات + معدات وصيانة وشارات — بنفس الممرضة (يتطلب `inventory:write` للكتابة).

#### 4) كتالوجات ومرضى ومهام — عرض أساسًا
- الكتالوجات عرض فقط (الإضافة/التعديل/الحذف `requireOwner` + "Only the Owner can manage"). المرضى/المواعيد قراءة (+ كتابة بالمنح). المهام كالكل (مع `taskType`).

### حدود الصيدلي
- لا فوترة/زيارات/طابور/اتصالات/موافقات/تدقيق/توفر. لا إعدادات/سوبر. أزرار الكتابة قد ترد 403 — راجع المالك.

---

## 8. المريض (Patient)

**بروفايل:** حساب منفصل (email + MRN + password) — ملفه وفواتيره ومواعيده فقط.
**أول شاشة:** `/patient-portal` (3 مواعيد + 3 تحاليل + علامات حية).

### الأقسام: النظرة العامة · مستنداتي · فواتيري ودفعها · مواعيدي · موافقاتي · التقييم · نماذج الدخول · الحجز العام

### المهام (8)

#### 1) الدخول — `/patient-login` (الثلاثة حقول → `POST /api/patient-auth/login`؛ الموقوف 401؛ جلسة 24h).

#### 2) الفواتير والدفع — `payNow` → `POST …/payments` → Stripe Checkout (يتطلب `STRIPE_SECRET_KEY` وإلا 503).

#### 3) إلغاء/تأجيل — بتأكيد → `POST …/cancel` / اختيار slot → `PATCH …/reschedule` (بلا حد ساعات في الكود؛ التعارض 409).

#### 4) الموافقات — Accept/Decline → `POST …/consents` (الأحدث يسود).

#### 5) التقييم — نجوم + `portal_rateSubmit` → `POST …/feedback` (مكتملة 90 يومًا، واحدة لكل زيارة).

#### 6) نماذج الدخول — `POST …/intake` (الإلزامي خادميًا).

#### 7) الحجز العام — `/book/[orgSlug]` (حتى +60 يومًا؛ العربون فاتورة sent).

#### 8) المستندات والتحاليل — عرض فقط (ملكية صارمة).

### حدود المريض
- 5 تبويبات `portal_comingSoon` معطلة. لا routes طاقم. الموقوف/المؤرشف مرفوض.

---

## جدول مقارنة سريع

| الدور | أول صفحة بعد اللوجن | عدد المهام الموثقة | الموديولز المتاحة |
|---|---|---|---|
| Owner | /dashboard (care-board) | 10 | كل الـ 25 |
| Super Admin | /super | 4 | أقسام المنصة الـ 7 (ليست موديولز عيادة) |
| Doctor | /dashboard (DoctorBoard) | 14 | 15 سريرية |
| Nurse | /dashboard (care-board) | 10 | 14 (+inventory بدل queue/audit) |
| Receptionist | /dashboard (ReceptionBoard) | 12 | 11 تشغيلية |
| Biller | /dashboard (care-board) | 10 | 10 مالية |
| Pharmacist | /dashboard (care-board) | 4 | 8 (labs/inventory/catalogs) |
| Patient | /patient-portal | 8 | 8 أقسام بوابة (ليست موديولز) |

## Permission Matrix كامل (موديول × دور — من `ROLE_MODULE_ACCESS` + RBAC الفعلي)

| الموديول | Owner | Doctor | Nurse | Receptionist | Biller | Pharmacist | Super Admin | Patient |
|---|---|---|---|---|---|---|---|---|
| dashboard | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ➖ (‎/super) | ➖ (بوابة) |
| patients | ✅ | ✅ | ✅ | ✅ | ✅ | ✅* | ➖ | ➖ (ملفه فقط) |
| appointments | ✅ | ✅ | ✅ | ✅ | ✅* | ✅* | ➖ | ➖ (مواعيده) |
| queue | ✅ | ✅ | ❌ (proxy يسمح، API يرفض) | ✅ | ❌ | ❌ | ➖ | ➖ |
| encounters | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ | ➖ | ➖ |
| analytics | ✅ | ✅ | ✅ | ❌ | ✅ | ❌ | ➖ | ➖ |
| consents | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ | ➖ | ✅ (موافقاته) |
| audit | ✅ | ✅ | ❌ | ❌ | ✅ | ❌ | ➖ (تدقيق منصة) | ➖ |
| labs | ✅ | ✅ | ✅ | ❌ | ❌ | ✅* | ➖ | ➖ (نتائجه) |
| tasks | ✅ | ✅ | ✅ | ✅ | ✅ | ✅* | ➖ | ➖ |
| documents | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ | ➖ | ✅ (مستنداته) |
| reports | ✅ | ✅ | ✅ | ❌ | ✅ | ❌ | ➖ | ➖ |
| availability | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ | ➖ | ➖ |
| catalogs | ✅ | ✅* | ✅* | ❌ | ❌ | ✅* | ➖ | ➖ |
| communications | ✅ | ❌ | ❌ | ✅ | ❌ | ❌ | ➖ | ➖ |
| locations | ✅ | ❌ | ❌ | ✅* (عرض فقط) | ❌ | ❌ | ➖ | ➖ |
| waitlist | ✅ | ❌ | ❌ | ✅ | ❌ | ❌ | ➖ | ➖ |
| billing | ✅ | ❌ | ❌ | ❌ | ✅ | ❌ | ➖ | ➖ (فواتيره) |
| payments | ✅ | ❌ | ❌ | ❌ | ✅ | ❌ | ➖ | ✅ (الدفع) |
| inventory | ✅ | ❌ | ✅ | ❌ | ❌ | ✅* | ➖ | ➖ |
| automation | ✅ | ❌ (ظاهر ومحظور) | ❌ | ❌ | ❌ | ❌ | ➖ | ➖ |
| campaigns | ✅ | ❌ | ❌ | ❌ (ظاهر ومحظور) | ❌ | ❌ | ➖ | ➖ |
| settings | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ➖ | ➖ |
| plan | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ➖ (خطط المنصة) | ➖ |
| help | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ➖ | ➖ |

> مفاتيح: ✅ موديول ممنوح · ❌ غير ممنوح · ➖ خارج النطاق · قيود دقيقة: automation وcampaigns يظهران في السايدبار لبعض الطواقم لكن proxy يحصرهما بالمالك (ظاهر ومحظور) · Pharmacist (الكتابة تتطلب منح granular وإلا 403) · Doctor/Nurse catalogs (القراءة فقط — الكتابة Owner) · Receptionist locations (القراءة فقط).

